services:
  - type: web
    name: analisador-rivas
    runtime: python
    repo: https://github.com/thiagotqp/analisador-rivas
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
